PORT=3001
DATABASE_URL=mysql://root:root@localhost:3306/idea_board?allowPublicKeyRetrieval=true
# Comma-separated list of origins allowed by CORS. Defaults to the local
# Vite dev server if unset.
CORS_ORIGIN=http://localhost:5173
